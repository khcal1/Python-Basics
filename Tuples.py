#This program displays the basics of tuples

import os
import sys
import random


'''Unlike lists, once a tuple is created it cannot be changed
figure out a way to change the contents in the tuple. make sure it is printed as a tuple'''

toopull = (1, 2, 3)

print(toopull)
print('\n')

toopull = list(toopull)

toopull = [4, 5, 6]

toopull = tuple(toopull)

print(toopull)
